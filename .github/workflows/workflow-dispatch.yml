name: My manual build
on: [workflow_dispatch]

jobs:
  build:
    name: build job
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run a one-line script
        run: echo Hello world
